{
  "name": "studentevents",
  "version": "1.0.0",
  "description": "StudentEvents - Full-stack event ticketing platform",
  "main": "backend/database-server.js",
  "scripts": {
    "start": "cd backend && node database-server.js",
    "dev": "python -m http.server 8000",
    "api": "cd backend && node database-server.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pg": "^8.16.3",
    "bcryptjs": "^3.0.2",
    "jsonwebtoken": "^9.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
